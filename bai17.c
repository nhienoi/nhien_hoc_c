// Bạn hãy viết chương trình tạo ra 2 biến kiểu số nguyên a và b. Sau đó nhập giá trị cho a và b từ bàn phím và hiển thị ra màn hình:

// a + b = {P}

#include <stdio.h>
int main() {
    int a, b;
    scanf("%d", &a);
    scanf("%d", &b);
    printf("a + b = %d", a + b);
    return 0;
}
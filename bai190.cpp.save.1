#include <iostream>
using namespace std;

void type(int m[], int n);
void export(int m[], int n);
int Bsearch(int m[], int n, int x);

int main() {
	
	return 0;
}
void type(int m[], int n) {
	for (int i = 0; i < n; i++) {
		cout << "m[" << i << "] = :";
		cin << m[i];
	}
}

void export(int m[], int n) {
	for (int i = 0; i < n; i++) {
		cout << "m[" << i << "] = "<< m[i] << "\t";
	}
}
int Bsearch(int m[], int n, int x) {
	int left = 0;
	int right = n -	mid = (left + right) / 2;
	do {
		
	} while (left <= right)
}

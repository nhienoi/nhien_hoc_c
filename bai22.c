// tính phạm vi lưu trữ của các kiểu dữ lieu int
#include <stdio.h>
#include <math.h>
int main() {
    // so byte ^ (2 * 8 ) - 1;
    // printf("Chuong trinh tinh pham vi du lieu\n");
    // printf("Nhap kieu du lieu ban muon kiem tra\n ");
    // printf("Kieu int %d\n", sizeof(int));
    // printf("Kieu char %d\n", sizeof(char));
    int luythua, x, n;
    scanf("%d", &x);
    scanf("%d", &n);
    for(int i = 0; i <= n; i++) {
        x = x * ;

        //n = 4; 2 * 2 
    }
    printf("%d", luythua);
    // printf("Pham vi du lieu int %lld", )
    return 0;
}
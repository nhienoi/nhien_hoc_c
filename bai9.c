// Bạn hãy viết chương trình tạo ra 2 biến a và b kiểu số nguyên, gán giá trị cho a = 254 và b = 343. Sau đó hiển thị ra màn hình:

// a + b = {P}
// Với {P} là tổng của biến a và biến b.
#include <stdio.h>
int main() {
    int a = 254;
    int b = 343;
    printf("a + b = %d", a + b);
    return 0;
}